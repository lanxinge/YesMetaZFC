# 证明减负记录

## 源码基点与计数口径

- 本轮 Git 基点：`7195271ee67fcdb46c2e744a8e6b717c1e01c926`。
- 本次直接接续已校验并回写原 Drive 包的 `eebcc76c8a6bedacfd88e043b00a8a141e9209f0`。
- 本次起点为 954 个 Lean 文件、233,186 行；此前本轮累计已净减 3,963 行。
- 更早的容器恢复把 `96277bd` 的已核验源码内容建立为本地恢复基点 `5670c49`；
  该恢复提交不冒充原历史提交。
- 统计根模块及 `YesMetaZFC/` 中全部 `.lean` 文件，包含注释与空行，排除缓存、临时探针和文档。
- 新增公共引理的代码计入净减量；没有通过删除数学内容、改变 AST 或减弱合同取得减量。

| 指标 | 本轮开始 | 闭合性完成 | 理论包含完成 | 模型装配完成 | 语法代数完成 | 守卫回放完成 | 阶段映射完成 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Lean 文件 | 950 | 951 | 952 | 953 | 953 | 954 | 954 |
| Lean 总行数 | 237,149 | 235,812 | 234,814 | 234,285 | 233,758 | 233,381 | 233,186 |
| 非空行数 | 222,529 | 221,191 | 220,191 | 219,797 | 219,271 | 218,874 | 218,676 |

| 本次指标 | 接续基点 | 当前源码 |
| --- | ---: | ---: |
| Lean 文件 | 954 | 955 |
| Lean 总行数 | 233,186 | 233,053 |
| 非空行数 | 218,676 | 218,561 |

本次净减 **133 行**（非空行净减 **115**）；本轮累计净减 **4,096 行**。
此前模型闭包和环境整理净减 396 行、闭合性专项 941 行、理论包含专项 998 行、
失效辅助与模型装配专项 529 行、语法代数专项 527 行、守卫回放专项 377 行、阶段映射专项 195 行。
距离每轮 5,000 行目标还差 **904 行**，整轮尚未完成。

## 覆盖保持链与重复规格包装清理

本次改动 22 个既有 Lean 模块，新增 `RelationalInheritance`；新增模块的 85 行全部计入净减量。

- `CoveredExtension` 同时保存旧覆盖仍被新阶段覆盖、旧函数与关系图保持不变的证明。
  七个阶段各提供一次证书，`trans` 组合证书；最终传输的四个起点只消费组合结果，
  不再分别枚举全部符号并展开整条图定义。
- 删除 27 个已为定义相等的参数映射包装和 8 个可直接投影证书的图等式包装。
  仍保留原先不要求覆盖假设的无条件图等式，不把这些更强结论替换成有 guard 的版本。
- 删除结构／相关阶段的 11 个旧定义转发包装。实际调用直接从定义所在的 Syntax 或
  RoundTwo 阶段传输；原始定义、规格正文和最终公理闭句保留。
- 51 个函数规格陈述使用 `FunctionSpecification` 共用同一语义合同；五个继承接口
  通过 `specification_regraph` 一次传输完整规格。原 guard 仍在原位置；任意正文翻译相等
  的接口没有被加强为布尔覆盖可判定，也没有加入模型类型类或更强理论假设。

源声明比较确认 **421 个未迁移声明头**及 **166 个既有数据／计算定义**保持原内容。
对 51 个折叠陈述和 46 个移除包装，临时审计逐字重放原参数和返回类型，分别以新接口或
公共合同证明原声明：共 **97 项**，涵盖 **21 个模块**。这些探针不进入源码包。
改名迁移没有保留旧包装常量；旧数学合同仍可由原假设推得。

## 当前检查点的验证与性能

四项门禁均正常结束：

| 门禁 | 耗时 |
| --- | ---: |
| `lake --wfail build` | 468.020 秒 |
| `bash scripts/check-all.sh` | 0.553 秒 |
| `prove_auto_sweep --help` | 0.211 秒 |
| `prove_auto_sweep` 单目标审计 | 1.455 秒 |

严格构建 952 个任务通过；全部 955 个源模块、957 个模块任务与 322 个扫描工具任务
通过，零错误、零警告。实际扫描仍以 `Ordinal.transitive` 为目标，
结果是既有的 `not_closed` / `unknown`，无新增公理、禁止依赖或原生计算票据；
扫描结束不等同于自动证明成功。构建时间仅记录增量验证成本，不作全库提速比较。

隔离重建基点 `eebcc76` 所需的 57 个模块，受影响的旧依赖不借用新版缓存；
未受影响的依赖通过只读链接复用。对 22 个改动模块的 **352 个原公开定理**，
移除包装使用上述原合同证明作对应，其余使用当前定理。新旧传递公理依赖清单
**352 项完全相同**；没有新增依赖。
公共 `CoveredExtension.trans` 无公理依赖；`translation`、`transfer` 和
`specification_regraph` 只依赖既有 `propext`、`Quot.sound`。以上是本次改动范围的核验，
不冒称全库所有定理重新审计。

全部构建和审计结束后，固定 Lean 4.33.1、4 线程，分别使用隔离旧依赖与当前依赖，
交替检查两个直接受阶段链整理影响的模块，各三次。测量包括加载、elaboration 和内核检查，
期间没有并行构建或审计。

| 模块 | 原版墙钟中位数 | 新版墙钟中位数 | 墙钟变化 | 原版 CPU 中位数 | 新版 CPU 中位数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| PureRelatedStage | 32.804 秒 | 21.787 秒 | -33.6% | 89.467 秒 | 63.888 秒 |
| PureFinalTransfer | 3.413 秒 | 1.481 秒 | -56.6% | 7.411 秒 | 1.988 秒 |

已确认源码净减量；以上只反映这两个模块的局部测量，**不据此宣称全库提速**。
当前检查点证据对应文末源码指纹。

## 上一检查点：阶段映射与传输清理

本次迁移 26 个 Lean 文件，保留各阶段的数学规格、guard、解释图及实际模型构造。

1. `RelationalEnvironment.mapSortValues` 把参数列的递归依赖收窄到排序映射。
   `mapValues` 仍接收原解释和原参数列；其结果对任意签名、模型和参数列与旧递归相同。
   相同排序的阶段参数映射现为定义相等，消除 32 处重复参数列归纳，并去除多层映射等式的串联。
2. `RelationalTransfer.transfer_regraph` 在源与目标均实现实际图、正文纯翻译相等时
   直接传输任意开放规格。`transfer_covered` 复用它，原先接受任意翻译相等的接口
   没有被加强为必须通过某份布尔覆盖清单。函数保持复用 `function_regraph`，
   关系保持复用新增的 `relation_regraph`。
3. 只增加关系的阶段直接复用前阶段的函数存在唯一性；继承函数分支不再重写参数包装。
   两步正文保持先组合语法等式，再直接传输一次，不再两次往返模型语义。
4. 最终求值定义和三类公理模式直接消费公共传输；四类最终闭句接口共用
   `ModelClosure.templateEnv_nil`，不再各自证明两份空环境相等。

原有 **580 个定理／定义声明头**逐项保留，**199 个数据与计算定义**保持原内容。
唯一调整的既有计算定义为 `mapValues`：在临时审计中，对任意输入证明其与原算法一致，
该等价证明无公理依赖。新增一个排序映射函数和两个公共传输引理，均计入净减量。

| 范围 | 净减行数 |
| --- | ---: |
| RelationalEnvironment / RelationalSemantics / RelationalTransfer | -24 |
| 22 个阶段及相关辅助模块 | 207 |
| PureFinalTransfer | 12 |
| 合计 | **195** |

## 上一阶段映射检查点的验证与性能

- `lake --wfail build`：951 个任务通过，零错误、零警告，用时 244.710 秒。
- `bash scripts/check-all.sh`：全部 954 个源模块、956 个模块任务及 322 个扫描工具任务
  通过，用时 0.654 秒。
- 扫描工具 `--help` 通过，用时 0.100 秒；实际审计扫描通过，用时 1.122 秒。
  `Ordinal.transitive` 仍为 `not_closed` / `unknown`，没有新增公理、禁止依赖或原生计算票据；
  这表示扫描工具正常运行，不表示该目标已经被自动证明。

以上是中断恢复、修正闭句模型参数后，从已有缓存接续的完整门禁结果。构建耗时仅记录
本次验证成本，不与其他检查点不同缓存状态的耗时比较来宣称提速。
在独立依赖目录中重建旧基点所需的 90 个模块，随后比较 26 个改动模块中全部
373 个原公开定理。新旧传递公理依赖清单 **373 项全部相同**，没有增加或移除依赖。
旧版没有借用受本次修改影响的新版依赖；未受影响的依赖只以只读链接复用。
这是上述改动模块的审计，不冒充全库逐定理审计。

在全部构建和公理审计结束后，使用 Lean 4.33.1、4 线程和默认检查选项，分别加载旧基点
`b4fcead` 与当前源码各自的依赖缓存，对下列四个模块交替检查各三次，期间没有并行构建。
测量包含模块加载、elaboration 与内核检查；旧版使用隔离重建的依赖目录，新版使用正常
构建目录。因此结果反映所测模块及其公共依赖修改的共同影响，不是全库重新构建对照。

| 模块 | 原版墙钟中位数 | 新版墙钟中位数 | 墙钟变化 | 原版 CPU 中位数 | 新版 CPU 中位数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| PureSequenceStage | 20.338 秒 | 20.181 秒 | -0.8% | 31.882 秒 | 31.200 秒 |
| PureRelatedStage | 33.828 秒 | 31.993 秒 | -5.4% | 91.605 秒 | 86.733 秒 |
| PureCompletedStage | 29.380 秒 | 28.959 秒 | -1.4% | 42.700 秒 | 41.632 秒 |
| PureFinalTransfer | 3.584 秒 | 3.545 秒 | -1.1% | 7.637 秒 | 7.623 秒 |

相关语法集合阶段的墙钟和 CPU 中位数分别下降约 5.4% 与 5.3%；其余三个模块墙钟
降幅均在 1.5% 以内，接近测量波动。**已确认净减量与上述局部样本结果，尚未确认全库
提速。** 本节证据对应上一检查点指纹 `5a316c431f4e4a566230600cbef26c207bd34e313b06c7a8145db4694da9f4f9`。

## 守卫与证书回放的重复取证清理

| 文件 | 原行数 | 当前行数 | 净减 |
| --- | ---: | ---: | ---: |
| DAGCertificate/IntrinsicReplay/Core | 119 | 121 | -2 |
| DAGCertificate/IntrinsicReplay/Local | 125 | 117 | 8 |
| DAGCertificate/IntrinsicReplay/Semantics | 1,348 | 1,081 | 267 |
| HOAvatarSoundness | 2,008 | 1,912 | 96 |
| HODAGCertificate | 938 | 850 | 88 |
| GuardSemantics（新增） | 0 | 80 | -80 |
| 合计 | 4,538 | 4,161 | **377** |

1. **FO 与 HO 重复维护同一命题语义。** `GuardSemantics` 统一文字取反、子句规范化、
   追加与逐文字赋值传输，证明守卫学习子句的语义等价及激活子句的可靠性。
   调用方仍须证明原守卫上两种赋值真值相同；没有把 atom map 外部条件删除。
2. **父快照、数组映射反复拆箱。** `parent_compiled_raw_of_snapshot` 从原已检查快照恢复
   同一父子句，`literalLinks_satisfies` 统一已编译真文字的链接映射。
   成员检查直接使用 Lean 的 `Array.all_eq_true'`，不再先造一个只用于取证的数组索引。
3. **每条 HO 规则重复装配同一外壳。** 规则检查和结论存在性集中处理，分支保留各自
   语义证明；替换、重命名仍验证环境良排序及固定 bound stack，扩张规则仍消费原合同。
4. **已知父节点存在后仍重演查找失败。** 使用已有索引查找等式，直接检查实际父节点的
   payload；原先由存在证据排除的 `none` 分支与重复节点相等证明不再出现在消费层。
   同时简化初始证明列表的递归：各分支只提取实际需要的首项或尾项证据。

五个既有文件的 **241 个定理／定义声明头**逐项保留，**124 个数据与计算定义声明**
保持原内容；新增 8 个公共引理。原数学定理、证书格式、检查算法、失败行为、
FO 排序与上下文、HO bound stack、guard 和可信前提均保留。

## 守卫回放检查点的验证与性能

在隔离目录中重建旧基点所需的 13 个模块，分别检查五个改动模块中的 134 个原公开
定理。新旧传递公理依赖清单 **134 项全部相同**，没有增加或移除依赖；这不是全库
重新审计，也不以新版依赖代替旧版依赖来作比较。新公共接口没有新增 `sorry`、
`admit`、自定义公理或原生计算可信依赖。

- `lake --wfail build`：951 个任务通过，零错误、零警告，用时 1,137.217 秒。
- `bash scripts/check-all.sh`：全部 954 个源模块、956 个模块任务和 322 个扫描工具任务
  通过，用时 10.063 秒。
- 扫描工具 `--help` 通过，用时 0.094 秒；实际审计扫描通过，用时 2.114 秒。
  `Ordinal.transitive` 的状态仍是 `not_closed` / `unknown`；没有新增公理、禁止依赖或
  原生计算票据。这表示扫描器正常运行，不表示该目标已获自动证明。

全部构建和公理审计完成后，使用 Lean 4.33.1、4 线程、同一当前依赖缓存、默认选项，
对旧基点和最终新版源码交替检查各三次，没有并行构建。测量包括模块加载、elaboration
和内核检查；只比较这些模块，不代表整个导入图重新构建的成本。

| 模块 | 原版墙钟中位数 | 新版墙钟中位数 | 墙钟变化 | 原版 CPU 中位数 | 新版 CPU 中位数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Semantics | 1.949 秒 | 1.772 秒 | -9.1% | 3.999 秒 | 3.460 秒 |
| HODAGCertificate | 3.120 秒 | 2.941 秒 | -5.8% | 4.499 秒 | 4.320 秒 |
| HOAvatarSoundness | 3.337 秒 | 3.490 秒 | +4.6% | 8.778 秒 | 9.479 秒 |

一阶回放和高阶证书模块的墙钟、CPU 样本中位数下降；HO-AVATAR 的墙钟上升约 4.6%，
CPU 上升约 8.0%。**已确认净减量，局部性能有升有降，尚未确认全库提速。**
构建时长只记录验证成本，不与上一批不同依赖范围的构建比较来宣称收益。
本节证据对应守卫回放检查点 `b4fcead`，源码指纹为
`2f6dbf0e07849365d328331318440082e440436fd677441c7472b2838a5625f2`，不代表后续阶段扩张重构的验证。

## 语法代数检查点的重复特征

| 文件 | 原行数 | 当前行数 | 净减 |
| --- | ---: | ---: | ---: |
| Derivation/Substitution/Basic | 1,955 | 1,517 | 438 |
| Derivation/Substitution/Algebra | 112 | 125 | -13 |
| FreshVariable | 875 | 803 | 72 |
| ProofT/SyntaxSubstitution | 240 | 225 | 15 |
| ProofT/SyntaxCodeRenaming | 108 | 93 | 15 |
| 合计 | 3,290 | 2,763 | **527** |

1. **只改变变量像却重复遍历 AST。** 项、参数列和公式统一使用任意 bound/free 重命名与
   替换的对应，再复用已有组合定律。两／三层弱化、编码层重命名后的替换及自由尾映射
   不再重复维护结构归纳。两类原本可直接证明的项／参数列交换律仍使用构造子同余，
   同时删去重复的 `change` 展开，保持其较小的公理依赖。
2. **把变量映射的完整类型与表达式抄写多遍。** β 往返、槽位交换、规范打开与重新抽象
   先按原目标匹配组合形状，再通过 `congr` 和逐变量等式完成；不再手写多屏映射等式。
3. **全称与存在量词重复证明同一个抽象交换律。** 新的
   `Formula.substituteMapped_abstractFreeTop_lift` 覆盖任意替换、排序与上下文，
   两种量词各自只接构造子。公式自由弱化的尾映射合同补入 `Substitution/Algebra`。

五个文件的原定理与定义声明头逐项保留；新增 6 个公共引理，不删除原声明。
原公式 AST、替换／重命名执行器、闭项嵌入算法、参数顺序与量词条件不变。
源码没有新增 `sorry`、`admit`、自定义公理或原生计算可信依赖。

## 语法代数检查点的验证与性能

本节保留 `b887d6c` 检查点的历史证据，不代表后续守卫回放重构的验证。

- `lake --wfail build`：950 个任务通过，零错误、零警告，用时 1,461.34 秒。
- `bash scripts/check-all.sh`：全部 953 个源模块、955 个模块任务及 320 个扫描工具任务通过，
  用时 74.60 秒。
- 扫描工具 `--help` 通过，用时 0.243 秒；一次实际目标审计扫描通过，用时 1.630 秒。
  目标仍为 `Ordinal.transitive`，结果仍是 `not_closed` / `unknown`，没有新增公理、
  禁止依赖或原生计算票据；这不表示该目标已被自动证明。

本次在恢复工具链、重建大部分缓存后验证；此前已编译过相关依赖模块。
上述时长只说明本次验证成本，不能与前几批的增量构建时间比较来宣称全库提速。

替换基础层 94 个原定理分别在新旧源码中重新检查传递公理依赖。
92 个清单相同；`Term.substituteMapped_weakenFree_boundId` 和对应的 `Arguments` 定理
从仅依赖 `propext` 变为无公理依赖。没有新增依赖；这项检查不冒称为全库重新审计。
最初候选把无公理的项交换律改成了依赖外延性的组合证明，已改回更短的直接构造子证明。

全部验证结束后，在 Lean 4.33.1、4 线程、相同当前依赖和默认选项下，交替检查原版与
最终新版源码，各三次，没有同时运行其他构建。测量包含模块加载、elaboration 和内核检查；
旧源码来自本次校验后的恢复基点，新增公共引理的源码行数已经扣除。

| 模块 | 原版墙钟中位数 | 新版墙钟中位数 | 墙钟变化 | 原版 CPU 中位数 | 新版 CPU 中位数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Derivation/Substitution/Basic | 2.100 秒 | 1.349 秒 | -35.8% | 4.876 秒 | 3.307 秒 |
| FreshVariable | 1.022 秒 | 0.975 秒 | -4.6% | 2.107 秒 | 2.139 秒 |
| ProofT/SyntaxCodeRenaming | 1.394 秒 | 1.594 秒 | +14.4% | 1.923 秒 | 1.999 秒 |

基础替换模块的墙钟样本中位数下降约 35.8%，CPU 时间下降约 32.2%。新鲜变量模块的
墙钟时间小幅下降，编码重命名模块上升约 14.4%；消费层仍有波动。
**已确认源码减量和基础模块的本次样本收益，尚未确认全库提速。**
早期候选的计时不作为最终结果；本节对应旧源码指纹
`c58b5b9ec9fed5945b423e1768bf57b95a6837c568cbf8f8858c789cc6764a38`。


## 模型装配检查点的低复用代码清理

| 类别 | 确认的冗余 | 净减行数 |
| --- | --- | ---: |
| 无调用的辅助与旧数据类型 | 心跳诊断函数、旧 `BinderKind`、停用的 lambda 参数生成器 | 15 |
| 无调用的定长特化与私有包装 | 六个二／三／四槽替换与弱化特例，三个固定元数存在量词缩写，重复数码不等式中转 | 195 |
| 重复模型装配 | 132 个手写私有 `*_models` 中转改为 118 项实际语义／子基证据；扣除 90 行公共实现 | 319 |
| 合计 | 13 个失效声明及一层重复装配 | **529** |

删除前检查原始文本中的名称引用、字段记法和字符串插值；不能把文本扫描结果直接当作死代码证据。
第一次严格构建发现 CDCL 的三个格式化函数仍被异常报告使用，已全部恢复且不计入减量。
扫描器的文件名与状态统计、动态导入模块，以及 `PureSourceLocalTests` 的实际语义对应均保留。
无当前消费者的序数、基数等数学定理不因此删除。

模型装配现在只维护真实闭句证据，有限公理基的结构只在 `SupportAxiomBasis` 中维护。
公共接口 `support_models` 及其原模型假设、完整原支撑理论和无限参数族保持不变。
共享子基仍产生普通内核证明；这项减量不是取消模型满足义务或移除所依赖的数学证明。

## 已完成的架构整理

- `derive_theory_subset weak ⊆ strong` 在理论组合边界派生普通包含定理。迁移原 229 个定理后，
  删除两项已无消费者的私有中转证明，并增加一个共享的 ZFC 幂集包含合同；现有 228 处派生。
  公共定理名、类型、`sentence` 与 `hSentence` 命名参数保留；公式和公理定义不变。
- `theory_inclusion` 只沿理论并、插入和别名构造结构性包含证明，并缓存已访问节点。
  源理论为并时必须覆盖所有分支；不能用它消掉额外公理，也不负责模型满足或数学推导。
  未确定的目标理论要求显式类型注解，避免消费层的类型推断边界丢失。
- 12 段消费层包含链使用公共接口；有限序列支撑统一组装后，算术联合理论直接弱化该实例。
  六个 ZFC 消费模块共用 `intrinsic_zfc_contains_power`，移除重复私有证明。
- `derive_free_closed f` 为指定公式构造派生 `f_freeClosed` 普通定理；共 121 处使用。
  保留原参数、深度、阶段和任意有序对约定，按项、参数向量及公式输入生成闭合性前提。
  每个构造只展开自身一层，子公式使用已有定理；生成的证明由 Lean 内核检查。
- 一元、二元及其 `Delta0` 模板在构造时自动检查 `freeClosed`。130 处手填字段由默认检查替代，
  另一个按坐标分类的模板保留两分支分析并复用公共规则。证书字段仍然存在。
- 参数向量构造、binder 弱化和模板实例化复用公共合同。序数归纳闭句及默认平坦有序对编码
  使用同一接口。原公式 AST、自由变量类型、集合论假设和理论适用范围不变。
- 此前 396 行净减量来自 `ModelClosure.close_of_curried`、参数代入环境等式和三个结构证明整理。
  使用说明见 [ENGINEERING.md](ENGINEERING.md)。

## 模型装配检查点的验证与性能

- `lake --wfail build`：950 个任务通过，零错误、零警告。
- `bash scripts/check-all.sh`：全部 953 个源模块、955 个模块任务及 320 个扫描工具任务通过。
- 扫描工具 `--help` 与实际审计扫描通过；目标 `Ordinal.transitive` 仍为
  `not_closed` / `unknown`，无新增公理或原生计算票据。这不是自动证明成功的声明。
- 临时边界检查通过：缺少公理证据、使用另一模型的证据均被拒绝；同一模型的合取证据、
  已验证子基以及重复子基装配均可完成。探针不进入源码包。
- 新旧 `support_models` 的完整类型定义相等；在相同当前依赖下重新检查旧源码，
  两者传递公理依赖清单完全一致。既有原生计算证书保留，不据此宣称全库重新完成可信审计。

最终固定源码的严格构建用时 1,551.78 秒，全源与扫描工具检查用时 36.32 秒。
其中部分依赖已由第一次发现 CDCL 诊断函数仍在使用的构建检查过；这些时间仅表示
当前验证成本，不能用于宣称全库提速。恢复该文件后重新运行了全部验证关卡。

完整构建结束后，在 Lean 4.33.1、4 线程、相同依赖与默认选项下，交替检查
提交 `d00599c` 的原装配模块与新模块，各三次；没有并行构建。包含模块加载、
elaboration 和内核检查，公共模块已编译，公共实现的源码行数已扣除。

| 模块 | 原版墙钟中位数 | 新版墙钟中位数 | 原版 CPU 中位数 | 新版 CPU 中位数 |
| --- | ---: | ---: | ---: | ---: |
| ZFC/PureSupportModels | 3.317 秒 | 3.657 秒 | 6.288 秒 | 3.738 秒 |

本次样本的 CPU 时间下降约 40.5%，墙钟时间上升约 10.3%。已确认源码减量，
**尚未确认实际等待时间缩短或全库提速**；不把较少的 CPU 工作量等同于更短的墙钟时间。

## 理论包含检查点的验证与性能

- `lake --wfail build`：949 个任务通过，零错误、零警告。
- `bash scripts/check-all.sh`：全部 952 个源模块、954 个模块构建任务及 320 个扫描工具任务通过。
- 扫描工具 `--help` 和一个实际目标审计扫描正常结束。目标仍为 `Ordinal.transitive`，
  状态为 `not_closed` / `unknown`，没有新增公理或原生计算票据；这不代表该目标被自动证明。
- 临时接口检查通过：理论并重排、空理论包含、错误删除公理分支的拒绝、命名参数兼容，
  以及既有 `as` 变量的语法兼容。命令用已有符号 `=>` 指定名称，不额外占用 `as`。
- 两项源层包含合同的 `#print axioms` 结果为空。ZFC 幂集合同与原手写包含链的依赖清单一致，
  包括目标理论类型中既有的 ZFC 语法核验证书；没有据此重新宣称全库可信依赖审计。

最终源码的增量严格复核用时 28.20 秒，全源检查用时 27.23 秒。
此前主构建已编译大部分依赖，末端发现并修正了一个局部 `omit` 作用域问题；以上时间是
修正后的最终复核成本，不能作为全库提速数据。

在完整验证结束、没有并行构建时，使用相同当前依赖交替检查提交 `4509b7f` 的原模块
和新模块，各三次；Lean 4.33.1、4 线程、相同默认检查选项，包含加载、elaboration 和内核检查。
新增公共接口的源码成本已计入净减量；以下比较只隔离各模块自身的变化。

| 模块 | 原版墙钟中位数 | 新版墙钟中位数 | 变化 | 原版 CPU 中位数 | 新版 CPU 中位数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| RelationProperties/Core | 2.769 秒 | 2.180 秒 | -21.3% | 5.232 秒 | 4.219 秒 |
| ArithmeticEvaluation | 1.586 秒 | 1.802 秒 | +13.6% | 1.655 秒 | 1.894 秒 |
| ZFC/IntrinsicKernel | 1.265 秒 | 1.625 秒 | +28.5% | 1.347 秒 | 1.772 秒 |

关系基础模块的样本中位数下降，算术支撑和 ZFC 内核模块上升；样本仍有波动。
本次确证了源码减量，**尚未确证全库提速**。后续应优先检查深层包含派生的搜索成本，
考虑复用已经检查的中间包含合同，再测量消费层收益。

## 闭合性检查点的验证

- `lake --wfail build`：948 个任务通过，零错误、零警告；该检查点增量验证用时 1,301.92 秒。
- `bash scripts/check-all.sh`：覆盖全部 951 个源模块，953 个模块构建任务及 320 个扫描工具任务通过。
- 扫描工具 `--help` 通过；实际审计扫描 `YesMetaZFC.SetTheory.Ordinal.transitive` 一个目标并正常结束。
  自动化状态仍为已有的 `not_closed` / `unknown`，不将工具运行成功当作自动证明成功。
两项临时反向检查已经确认：含自由变量的模板无法补出证书；主动引入自由变量的缩写无法派生合同。
该检查点没有新增 `sorry`、`admit` 或公理；闭合性派生命令拒绝含未完成证明的结果。

## 闭合性专项的类型检查测量

固定 Lean 4.33.1、4 个线程，用 `lake env lean <文件>` 检查模块；测量包括模块加载、
elaboration 和内核检查，依赖已经构建。

首次在改动前后各测三次：函数空间模块的中位数为 2.72 → 4.04 秒，序数语法模块为
1.74 → 2.49 秒；其中新版函数空间的单次墙钟时间从 2.12 到 5.66 秒，波动很大。
为核对这一现象，又在**相同当前依赖**下交替检查原模块源码与新模块源码，各三次；
两侧同样关闭旧源码可能触发的 unused-simp-argument 提示，仅用于这个对照。

| 模块 | 原版墙钟中位数 | 新版墙钟中位数 | 变化 | 原版 CPU 中位数 | 新版 CPU 中位数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| FunctionSpaceAlgebra | 2.436 秒 | 2.260 秒 | -7.3% | 4.169 秒 | 3.809 秒 |
| Project/Ord/Syntax | 2.668 秒 | 2.882 秒 | +8.0% | 3.028 秒 | 3.235 秒 |

同条件对照中，函数空间消费层更快，序数公共层的中位数仍较慢；测量仍有波动。
因此闭合性专项确证了源码减量，**尚未确证全库提速**。这个对照只隔离两个模块自身的变化，
不能代表全部公共依赖的成本；后续需继续测量和优化公共层派生开销。
全库构建用时仅是当前验证成本，不能与恢复时的冷构建直接比较。

## 恢复与保存

规范源码包沿用 [YesMetaZFC-source.zip](https://drive.google.com/file/d/1b9gV-m5sT7DEVhgNNSzsV9ZrkpzYeUID/view)
的同一文件身份。包内只保留源码、配置、文档及既有检查脚本。
当前重构基于上述 Git 提交；恢复本次减负成果应使用该包，不能仅凭远端 `main` 推断包含本次改动。
工具链及恢复步骤见 [RESTORE.md](RESTORE.md)。

Lean 源码指纹（按相对路径排序，依次哈希 `路径 + NUL + 文件原始字节 + NUL`）：

```text
f9d59bf79e0e01806f911073cc7245ad867a40d79cfbf20a7b4ed0ef2e55099a
```
